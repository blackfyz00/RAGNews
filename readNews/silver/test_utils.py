# from utils import *

# bronze_file = PROJECT_ROOT / "readNews" / "test.txt"
# silver_file = PROJECT_ROOT / "silver" / "silver_test.json"

# news = load_json(bronze_file)

# for item in news:
#     item["id"] = generate_news_id()

# save_json(news, silver_file)